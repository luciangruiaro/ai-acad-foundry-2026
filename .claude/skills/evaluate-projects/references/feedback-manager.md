# Feedback template — manager

Third person. No marks, no file paths, no code. Readable by someone who was not on the
course and will not open the repository. Reports **capability demonstrated**, never
capability absent — this letter lands in an employment relationship the course cannot
see into, so nothing in it may read as a performance problem. Draws only on the
repository and the five project answers: nothing from the form's feedback questions
(module ratings, opinions, learning wishes) ever appears here. A weaker submission
makes this letter shorter and more factual — not warmer, not invented. Length: half a
page.

---

## `<Full name>` — AI Engineering on Azure · project summary

**Module:** AI Engineering on Azure, Libra Bank Academy (Digital Stack for Libra Bank),
July 2026 — seven sessions covering Python for AI systems, generative-AI concepts,
retrieval-augmented generation, Microsoft Azure AI Foundry, agents, and container
deployment. The module closes with an individual project: each participant extends a
working AI assistant with their own AI agent, their own knowledge base with a
self-built quality-measurement set, and their own user interface, on Azure.

**What `<first name>` built:** `<2–3 sentences in plain language: the assistant's
domain, what the agent does, what the knowledge base covers, what the interface lets a
user do. Concrete and jargon-free — "an assistant that answers questions about X,
with an agent configured to respond as Y, and a web interface showing which sources
each answer came from.">`

**Capabilities demonstrated:** `<3–4 bullets, each a capability with its evidence in
plain words. Choose only what the evidence supports, e.g.:>`
- Built and configured an AI agent on Microsoft Azure and integrated it into a working
  application.
- Designed a knowledge base and — notably — a way to *measure* the assistant's answer
  quality against known-correct answers, then used the measurements to improve it.
- Delivered a working user interface making the system's reasoning visible to users.
- Worked in the industry-standard way: version control, documented setup, configuration
  separated from code, AI coding tools used deliberately.

**How they worked:** `<1–2 sentences on process visible in the work: iterated in small
steps, documented as they went, tested before claiming, gave an accurate account of the
system's current limits — that last one phrased as the professional virtue it is.>`

**A natural next step:** `<one sentence, framed as growth, never as gap: the kind of
work that would build on what they demonstrated — e.g. "taking a system like this
through a production deployment" or "applying the same measurement discipline to a
larger document base". This is guidance for the manager on what to hand them next.>`

`<sign-off, instructor name and module title>`
